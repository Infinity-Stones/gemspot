/**
 * 네이버 지도 JS API 클라이언트 ID.
 *
 * 브라우저가 스크립트를 직접 받아오므로 요청 URL에 그대로 실린다 — 숨길 수
 * 없는 값이라 환경 변수로 두지 않고 여기 박는다. 환경 변수로 두면 팀원마다
 * `.env.local`을 만들어야 하는데, 감춰지지도 않는 값에 그 비용을 치를 이유가
 * 없다. 오남용은 네이버 콘솔의 도메인 등록으로 막는다.
 */
export const NAVER_MAP_CLIENT_ID = 'jgxxww8q6w';
