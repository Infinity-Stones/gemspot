# gemspot — 시각 기준

> Ant Design의 규칙을 따르되 프라이머리는 보라(#7a40ed)다. 라이브러리를 쓰지 않고 토큰으로만 옮긴다.

**기준:** Ant Design v5 · 라이트 기본 · 다크 대응

이 문서가 화면의 단일 기준이다. 여기 없는 값은 `panda.config.ts`에 등록하지 않고, 화면에서도 쓰지 않는다.

## 왜 Ant인가

화면마다 버튼의 테두리 굵기와 모서리와 높이가 조금씩 달랐다. 그 차이가 쌓이면 사용자는 무엇이 주 동작인지 화면마다 다시 읽어야 한다. Ant은 그 판단을 이미 끝내 둔 체계다 — 위계 셋, 모서리 둘, 그림자 셋. 우리는 그 규칙만 가져오고 색은 우리 것을 쓴다.

## 토큰 — 색

프라이머리는 **지도 위 현재 위치 점의 보라**다. Ant의 색 생성 규칙을 그 색에 적용해 10단계를 만든다. 6이 기준, 5가 hover, 7이 active다.

| 단계 | 값 | 쓰임 |
|------|-----|------|
| primary.1 | `#f6f0ff` | 태그·선택된 행의 옅은 바탕 |
| primary.2 | `#ead9ff` | 옅은 구분선 |
| primary.3 | `#d5bbfc` | 프라이머리 계열 테두리 |
| primary.5 | `#a172f3` | hover |
| **primary.6** | **`#7a40ed`** | **주 동작·현재 위치·링크·포커스 링** |
| primary.7 | `#5f2bcc` | active, 밝은 바탕 위 글자 |

중립은 Ant의 회색 축을 그대로 쓴다.

| 단계 | 값 | 쓰임 |
|------|-----|------|
| gray.1 | `#ffffff` | 카드·입력 표면 |
| gray.2 | `#fafafa` | 카드 안의 한 단 들어간 면 |
| gray.3 | `#f5f5f5` | 페이지 지면 |
| gray.4 | `#f0f0f0` | 구분선·카드 테두리 |
| gray.5 | `#d9d9d9` | 입력·버튼 테두리 |
| gray.8 | `#595959` | 보조 글자 |
| gray.9 | `#1f1f1f` | 본문 글자 |

상태색은 Ant 기본값이다. 성공 `#52c41a` · 오류 `#ff4d4f`(바탕 `#fff2f0`, 테두리 `#ffccc7`) · 정보 `#1677ff`(바탕 `#e6f4ff`).

### 시맨틱 토큰

화면은 원색을 직접 쓰지 않고 `ui.*`만 쓴다. 라이트/다크 짝이 여기서 한 번에 묶인다.

`ui.canvas` 지면 · `ui.surface` 카드 · `ui.muted` 한 단 들어간 면 · `ui.ink` 본문 · `ui.subtle` 보조 글자 · `ui.border` 컨트롤 테두리 · `ui.line` 카드 테두리·구분선 · `ui.accent`/`ui.accentHover`/`ui.accentActive` 프라이머리 3단 · `ui.onAccent` 그 위의 글자 · `ui.accentText` 밝은 바탕 위 프라이머리 글자 · `ui.danger`/`ui.dangerBorder`/`ui.dangerWash` 위험 3단.

## 토큰 — 타이포

본문 **14px**이 기준이다. 그 위로만 올라간다.

| 역할 | 크기 | 행간 | 굵기 |
|------|------|------|------|
| caption | 12px | 1.67 | 400 |
| bodySm | 13px | 1.6 | 400 |
| body | 14px | 1.57 | 400 |
| bodyLg | 16px | 1.5 | 400 |
| button | 14px | 1.57 | 500 |
| subheading | 16px | 1.5 | 600 |
| headingSm | 20px | 1.4 | 600 |
| heading | 24px | 1.35 | 600 |
| headingLg | 30px | 1.27 | 600 |
| display | 38px | 1.21 | 600 |

제목 바로 아래 한 문장은 `bodyLg`, 그 아래 보조 안내는 `body`다. 둘을 같은 크기로 두면 무엇을 먼저 읽을지가 사라진다.

Ant의 작은 글자는 12px이지만 한글은 그 크기에서 획이 뭉친다. 화면에서 가장 많이 쓰는 단계라 `bodySm`만 13px로 한 칸 올렸다.

서체는 Inter 하나다. 제목용 서체를 따로 두지 않는다 — 크기와 굵기로 이미 위계가 선다.

## 토큰 — 모서리·높이·그림자

| 대상 | 값 |
|------|-----|
| 버튼·입력·작은 면 | 6px (`radii.control` · `radii.input`) |
| 카드·패널·모달 | 8px (`radii.panel`) |
| 태그·배지 | 4px (`radii.badge`) |

캡슐(알약) 모서리는 쓰지 않는다. 예외는 **지도 위에 떠 있는 표식** 하나다(홈의 저장 건수) — 지면 위 컨트롤이 아니라 떠 있는 것이고, 지도 UI의 관습이기도 하다.

컨트롤 높이는 **44px**(`sizes.control`)이다. Ant의 기본은 32/40px이지만 이 앱은 모바일 화면이고, 터치 타깃 최소 44×44px이 전역 접근성 기준이라 그쪽을 따른다. 큰 컨트롤만 48px(`sizes.controlLg`).

그림자는 셋뿐이고, **떠 있는 것만** 든다.

| 이름 | 쓰임 |
|------|------|
| `card` | 거의 눕는다. 지금은 쓰는 곳이 없다 — 카드는 그림자가 아니라 1px 선으로 경계를 만든다 |
| `floating` | 화면 위에 뜬 것: 모달·토스트·플로팅 버튼·지도 위 알림 |
| `preview` | `floating`과 같은 값. 더 큰 오버레이가 필요해지면 여기서 갈린다 |

카드에 그림자를 주면 무엇이 위에 있는지가 사라진다. 그것이 이 문서를 다시 쓴 이유 중 하나다.

## 컴포넌트 규칙

구현은 `src/components/uiStyles.ts` 한 곳에 있다. 화면에서 버튼이나 입력을 다시 그리지 않는다.

### 버튼 — 위계는 셋뿐

| 종류 | 모양 | 언제 |
|------|------|------|
| **primary** | 프라이머리 채움, 흰 글자 | 화면에 **하나**. 이 화면을 끝내고 다음으로 넘기는 동작 |
| **default** | 흰 바탕 + 1px 테두리, hover에 프라이머리 테두리·글자 | 화면 안에서 끝나는 동작. 여럿 나란히 둘 수 있다 |
| **text** | 테두리·바탕 없음 | 취소·닫기처럼 물러나 있어야 하는 것 |
| **dangerText** | 테두리 없이 빨간 글자, hover에 옅은 빨강 바탕 | 삭제처럼 되돌릴 수 없는 것 |

한 화면에 채움 버튼이 둘이면 둘 다 주 동작이 아니게 된다.

### 카드

`ui.surface` 바탕, `ui.line` 1px 테두리, 8px 모서리, 안쪽 여백 16px. 그림자 없음. 카드 안에 한 단 더 들어가는 면은 `ui.muted` 바탕에 테두리 없이 6px 모서리.

### 입력

흰 바탕, `ui.border` 1px, 6px 모서리, 높이 44px. hover에 테두리가 프라이머리로 밝아지고 focus에 프라이머리로 굳는다. 라벨은 `bodySm` 보조색으로 입력 위에 둔다.

### 읽는 값과 누르는 것

건수·상태 같은 **읽는 값에는 테두리도 알약 바탕도 두지 않는다.** 그 모양은 버튼의 것이고, 눌러도 아무 일이 없다는 걸 눌러 보고 알게 된다. 강조가 필요하면 색과 굵기로만 한다.

### 포커스

`:focus-visible`에 프라이머리 2px 아웃라인, 오프셋 2px. 전역에서 한 번만 정의한다.

## 접근성

전역 기준(`~/AGENTS.md`)이 그대로 적용된다. 여기서는 이 체계와 부딪히는 지점만 적는다.

- 터치 타깃 44×44px이 Ant의 컨트롤 높이보다 우선한다.
- 상태를 색으로만 구분하지 않는다. 건수는 숫자로, 성공/실패는 글자로 함께 적는다.
- 본문 대비 4.5:1. `ui.subtle`(#595959)은 흰 바탕에서 7:1로 이 선을 넘는다. 그보다 흐린 회색을 본문에 쓰지 않는다.
